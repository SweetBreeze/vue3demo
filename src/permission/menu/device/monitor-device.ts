import { MenuItem } from "@/permission/menutree";

let menu = new MenuItem(
    "device:monitor-device",
    "设备监控管理",
    "monitor",
    false,
    true,
    "/monitor-device"
);
export default menu;
